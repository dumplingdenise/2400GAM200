using UnityEngine;

public class timeLineController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
    /* For controlling the time switch mechanic
        Changing of time state
        Any condition for time state -> enable/disable of roots
        Cooldown timer etc.
     */

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
